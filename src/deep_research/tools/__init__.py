"""Tool registry and tool implementations (procedural memory).

`registry.py` (P4) holds machine-readable function definitions; researchers
resolve tools from here, never import them directly.
Tools: tavily_search (P1), wikipedia (P4), scraper (P4), semantic_search (P5).
"""

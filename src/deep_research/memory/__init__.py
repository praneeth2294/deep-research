"""Memory subsystem — the deck's four memory types.

checkpointing.py (P5) — durable short-term/working memory (resume, HITL).
episodic.py (P5) — past research sessions (vector DB).
semantic.py (P5) — cached sources / knowledge base (RAG).
Procedural memory lives in prompts/ and tools/registry.py.
"""
